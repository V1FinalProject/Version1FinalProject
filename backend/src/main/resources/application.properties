spring.application.name=spring-hello-world
server.port=8080

# Path to the mock nominations spreadsheet used by the tagging spike, relative to
# the directory the application is started from (backend/ when run via Maven).
tagging.nominations-file=../data/Star_Awards_Mock_Nominations.xlsx
